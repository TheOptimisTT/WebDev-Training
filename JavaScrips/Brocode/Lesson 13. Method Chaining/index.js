/*
    method chaining = calling one method after another
        in ne continuous line of code
*/

let userName = "bro";

/* no method chain 

let letter = userName.charAt(0);
letter = letter.toUpperCase();
*/
//with method chain

let letter = userName.charAt(0).toUpperCase();